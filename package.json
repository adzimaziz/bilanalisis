{
  "name": "kampung-kilowatt",
  "version": "1.0.0",
  "private": true,
  "scripts": {"build:3d": "esbuild game-3d-src.js --bundle --minify --format=iife --target=es2020 --outfile=game-3d.js --legal-comments=linked"},
  "dependencies": {"three": "0.180.0"},
  "devDependencies": {"esbuild": "0.25.10"}
}
